/**
 * Register phase - no permissions needed (Super Admin only)
 */
const register = ({ strapi }) => {
  // Plugin is Super Admin only - no permission registration needed
};

export default register;
